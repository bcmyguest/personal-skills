# ansible

| Skill | What it does |
|-------|--------------|
| `add-ansible-role` | Add a new tool-install role to a personal Ansible provisioning playbook. Location is read from the skill's `config.json`; ships a GitHub-release task template. |

Skills live in [`../skills/`](../skills); this plugin selects them via `skills` in the
[marketplace manifest](../.claude-plugin/marketplace.json).
