# 06 — Parts V.5, VI.2 and VI.3 reproducible from the library

**What to build:** The published tables that claim to have been re-measured
"through the library classes rather than the prototype" can actually be
regenerated by running the code on the branch. At present the superposition
experiment still defines its own private whitening and superposition helpers
and never imports the library ones, so those three sections cannot be
reproduced from what exists.

After this ticket, running the experiment reproduces the published numbers, or
the numbers change and are republished — either outcome is a result.

**Blocked by:** 04 — Whitener fitted on held-out data (so the port does not
bake in the in-sample fit discipline).

**Status:** ready-for-agent

- [ ] The superposition experiment uses the library's whitener, superposition
      and decode paths, with no private duplicates remaining
- [ ] Running it regenerates the Part V.5 / VI.2 / VI.3 tables, and any cell
      that moved is republished with the reason
- [ ] The prototype-versus-library equivalence is asserted by a test, so the
      two cannot silently diverge again
